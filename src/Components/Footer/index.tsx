import React from 'react'
import * as s from "./footer.style"

export const Footer: React.FC = (): JSX.Element => {
    return (
        <s.Wrapper style={{color: "#fff"}}>
            Footer
        </s.Wrapper>
    )
}
